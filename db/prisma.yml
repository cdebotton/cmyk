endpoint: http://localhost:4466
datamodel: datamodel.graphql
hooks:
  post-deploy:
    - prisma generate
    - graphqlgen
generate:
  - generator: typescript-client
    output: ../src/graphql/generated/prisma-client