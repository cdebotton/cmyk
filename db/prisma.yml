endpoint: http://localhost:4466
datamodel: datamodel.graphql
hooks:
  post-deploy:
    - echo "Deployment finished"
    - graphql get-schema --project prisma
    - yarn send-introspection-query http://localhost:3030/graphql